<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}

        {# Ici les liens vers CSS etc.. #}
        {% block stylesheets %}
            {# asset() pour faire référence au dossier public #}
            <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
            <link rel="stylesheet" href="{{ asset('css/style.css') }}">
        {% endblock %}

    </head>

    {# Ici on injecte les vues des block body.. #}
    <body>
        {# Exemple pour afficher une image #}
        <img src="{{ asset('img/logo.png') }}" alt="">
        <nav>
            <ul>
                {# ICI {{ path('app_entreprise')}} et app_entreprise est le name dans la route dans le controller #}
                <li> <a href="{{ path('app_entreprise') }}">Liste des entreprises</a></li>
                <li> <a href="{{ path('app_employe') }}">Liste des employés</a></li>
                <li>{# add_entreprise vient du name de la route dans EntrepriseController de la function add #}
                    <a href="{{ path('add_entreprise') }}">Ajouter une entreprise</a>
                </li>
                {# <li><a href="{{ path('add_employe') }}">Ajouter un employé</a> </li> #}
            </ul>
        </nav>

        {% block body %}{% endblock %}

        {# Ici les liens vers JS etc.. #}
        {% block javascripts %}
            <script src="{{ asset('js/script.js') }}"></script>
        {% endblock %}
    </body>
</html>
